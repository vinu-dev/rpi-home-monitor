[mutmut]
# Mutation test a small, security-critical surface so CI completes in < 15 min.
# Expand this list in follow-up PRs as kill-rate hardens.
paths_to_mutate=monitor/auth.py,monitor/password_policy.py,monitor/models.py
runner=python -m pytest tests/unit tests/integration -x -q
tests_dir=tests
